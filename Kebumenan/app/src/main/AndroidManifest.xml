<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kebumenan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".hasilkuis"></activity>
        <activity android:name=".kuis" />
        <activity android:name=".informasi" />
        <activity android:name=".wisata" />
        <activity android:name=".kebudayaan" />
        <activity android:name=".geografi" />
        <activity android:name=".sejarah" />
        <activity android:name=".tam_pertama" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>